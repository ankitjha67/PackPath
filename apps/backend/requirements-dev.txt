# Test-only dependencies. CI's pytest job installs requirements.txt
# plus this file; the production image installs requirements.txt only.
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1
fakeredis==2.26.1
